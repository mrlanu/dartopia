server:
  port: ${PORT:8282}

spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}
  cache:
    type: caffeine

myapp:
  jwtExpirationMs: 900000
  jwtAccessExpirationMs: 900000
  jwtRefreshExpirationMs: 604800000
  settings:
    server-name: ${SERVER_NAME:test1}
